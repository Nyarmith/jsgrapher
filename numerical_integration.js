function num_int(){
    //returns points to interpolate
    this.simpson_method = function(func, interval, granularity)
    {
    }

    //literally trapezoid method but animated with our graph object
    this.animated_method = function(func, interval, granularity, graph_obj)
    {
    }
}
